# Backend base URL. Use your computer's LAN IP when testing on a physical device.
EXPO_PUBLIC_API_URL=http://localhost:3000

# Temporary client-side planner configuration. Move provider credentials to the backend before release.
EXPO_PUBLIC_OPENROUTER_API_KEY=
EXPO_PUBLIC_OPENROUTER_MODEL=deepseek/deepseek-v4-flash
